m := 2;
n := 6;
g := 1;
F := Rationals();
R<u,v>:=PolynomialRing(F,2);
X := v^2 - u^3 + u^2 - u; // Elliptic Curve 24a4
q := v;
t := 4*v/(u*v^2-u-3*v^2-1);
E := [0,t^2-2*q*t-2,0,-(t^2-1)*(q*t+1)^2,0];
P := [R!0,0];
Q := [(t+1)*(q*t+1),t*(q*t+1)*(t+1)];